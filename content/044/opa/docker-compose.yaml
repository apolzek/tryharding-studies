services:
  opa:
    image: openpolicyagent/opa:0.69.0
    container_name: cncf-opa
    command:
      - run
      - --server
      - --log-level=info
      - --set=decision_logs.console=true
      - /policies
    volumes:
      - ./policies:/policies:ro
    ports:
      - "8181:8181"
