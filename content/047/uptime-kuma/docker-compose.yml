services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma-poc
    restart: unless-stopped
    ports:
      - "127.0.0.1:19001:3001"
    volumes:
      - kuma_data:/app/data

volumes:
  kuma_data:
