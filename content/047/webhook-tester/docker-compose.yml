services:
  webhook-tester:
    image: ghcr.io/tarampampam/webhook-tester:2
    container_name: webhook-tester-poc
    command: ["serve"]
    ports:
      - "127.0.0.1:19002:8080"
    restart: unless-stopped
